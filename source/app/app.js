/*

	// Bootstrap angular applications until dom is ready
	angular.element(document).ready(function() {
	    angular.bootstrap(document, ['myApp']);
	});

*/

angular.module('myApp', ['partial', 'content']);